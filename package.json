{
  "scripts": {
    "start": "npm install -g grunt-cli && npm install && grunt"
  },
  "devDependencies": {
    "express": "^4.13.4",
    "grunt": "^1.0.1",
    "grunt-contrib-connect": "^1.0.2",
    "grunt-contrib-cssmin": "^1.0.1",
    "grunt-contrib-uglify": "^1.0.1"
  }
}
